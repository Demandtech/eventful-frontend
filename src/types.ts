export type TicketType = {
  name: string;
  price: string;
};

export type Event = {
  _id: string;
  name: string;
  time: string;
  date: string;
  price: string;
  img: string;
  venue: string;
};